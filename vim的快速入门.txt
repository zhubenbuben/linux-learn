#正常模式：用vim打开一个档案直接进入的模式；插入模式：编辑模式（按下i,I,o,O,a,A,r,R）;命令行模式:对vim进行处理的模式（:或者/进入）

# ls 查看当前文件目录

#打开或创建henllo.txt文件；按下i进入编辑模式
vim hello.txt

# esc退出插入（编辑）模式；输入:进入命令行模式；:wq保存退出；:q退出；:q!强制退出且不保存
# yy 拷贝当前行；
# 5yy 拷贝5行；
# p 粘贴
# dd 删除当前行
# 5dd 删除当前5行
# /关键字 在命令行模式进行查找；输入n查找下一个
# : set nu 设置行号；: set nonu 取消设置行号
# G 进入文档末尾行；gg 进入文档首行
# u 撤销键入
# 20shift+g 将光标移动到第20行
